# Description #
This program allows to perform CI calculations for d orbitals,
by modifying number of electrons crystal fields and spin orbit coupling

# Disclaimer #
This is still a version under heavy development.

# How to install #
Execute the script install.py

# Usage #
execute "tranci" in a terminal

# Dependencies #
This library requires Python3, together with the libraries
  - numpy
  - scipy
  - matplotlib

the pdflatex command should be installed in order in the system

# Compatibility #
This program works both in Mac and Linux.

